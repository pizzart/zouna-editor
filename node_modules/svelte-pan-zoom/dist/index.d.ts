interface Point {
    x: number;
    y: number;
}
type Render = (ctx: CanvasRenderingContext2D, t: number, focus: Point) => void | boolean;
interface Options {
    width: number;
    height: number;
    render: Render;
    padding?: number;
    maxZoom?: number;
    friction?: number;
}
declare function panzoom(canvas: HTMLCanvasElement, options: Options): {
    update(options: Options): void;
    destroy(): void;
};

export { Options, Point, panzoom };
